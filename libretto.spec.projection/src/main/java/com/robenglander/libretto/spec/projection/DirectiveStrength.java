package com.robenglander.libretto.spec.projection;

/** Mirrors {@code StrengthValue} in {@code LibrettoSpec.xtext}. */
public enum DirectiveStrength {
	REQUIRED,
	STRONGLY_PREFERRED,
	PREFERRED,
	ADVISORY
}
